{% extends "base.html" %}
{% block content %}
  <h1>Sign in</h1>
  <form method="post" action="{% url 'login' %}">
    {% csrf_token %}
    {{ form.as_p }}
    <button type="submit">Login</button>
  </form>
  <p><a href="{% url 'logout' %}">Logout</a> (for testing)</p>
{% endblock %}
